name: "Build Slothchain 💤"

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Slothchain
        uses: ignite/cli/actions/cli@main
        with:
          args: chain build